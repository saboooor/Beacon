<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework theme keeps the app free of an appcompat dependency; all real styling is Compose. -->
    <style name="Theme.Beacon" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
