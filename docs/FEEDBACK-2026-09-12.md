# September 12 feedback and release audit

This is a fresh follow-up to [the September 7 audit](FEEDBACK-2026-09.md), based on GitHub
`main` at `cdcc73a`. All 24 issue bodies, all 27 pull-request bodies, 91 conversation comments,
seven inline review comments, submitted PR reviews, and five Discussions were fetched and read.
Discussion pagination was exhausted. Issue #29 had 21 replies and was read in full twice; it was
the only conversation with more than 12 replies. Replies were held until publication.

## Changes for 1.0.13 (version code 14)

- Root recovery now tolerates a departed helper's PID being reused by an unrelated process, without
  signaling that process. A different surviving helper still blocks recovery. AUTO can retry a
  recovered root destination only after exact source exit and before ADB cleanup has started.
- Setup has an optional Shizuku-loss notification. It remembers an actually working transport,
  stays off by default, requires notification permission, and avoids warnings during a working
  connection or active root startup. An intentional disconnect clears the remembered connection.
  The notification opens Setup and is excluded from HiLight's notification rules/reminders.
- An opted-in, previously connected Shizuku user gets a one-time delayed availability check after
  reboot. The receiver/job do not initialize Store, request root permission, or start a renderer.
- Seven patterns from @saboooor's PR #39 were adapted into the existing preview and device
  renderers: Meter, Strobe, Heartbeat, Bounce, Radar, Converge and Glitch. Existing duty, brightness,
  duration and cleanup limits remain. Renderer revision 8 forces replacement of older helpers.
- Compilation is explicitly pinned to Android 37.0, matching F-Droid's existing recipe even when
  Android 37.1 is installed locally. Package identity and release certificate remain unchanged.

## Every issue revisited

| Issues | Finding and disposition |
| --- | --- |
| #9 | Original retail-device evidence request is closed. The September 7 signed Pixel upgrade is documented in the earlier audit; that evidence does not verify September 12 root changes. |
| #11 | Release shrinking is already shipped. Retained R8 and both privileged entry points. |
| #17 | Camera/microphone activity rules already shipped. Preserve their activity-based meaning and one-minute limit. |
| #19 | Earlier elapsed-time heartbeat fix is retained. New exact-exit/root-retry changes are relevant, but KernelSU Next confirmation remains missing. |
| #22 | September 11 job 16441911893 finds 1.0.12/code 13, then fails because metadata still lists 1.0.7/code 8. The user explicitly deferred GitLab work on September 12; the submission was not updated. Catalog acceptance remains external. This is separate from the previously verified 1.0.7 signature-copy result. |
| #25 | Releasing idle light sessions restored system/Gemini effects. Existing release/cleanup tests retained. |
| #26 | While-open restoration and retained activity tracking already shipped. Existing watcher lifecycle retained. |
| #28 | Physical stuck-LED report remains unconfirmed on the affected Pixel 11 Pro. Preserve bounded black-only recovery and exact ownership fences. Do not claim a new hardware fix. |
| #29 | Read all 21 replies twice. Several reporters confirmed the Shizuku fix from 1.0.9/1.0.10. Requested delayed tests, face-down controls, schedules and typed values already exist. Preserve these behaviors. |
| #32 | Reporter confirmed Discord's first-alert fix in 1.0.11. Preserve the distinction between a screen wake and unlock. |
| #36 | Face-down mode and clearer settings groups confirmed. Long-term battery impact still needs physical measurement. |
| #37 | Whole-app rule copying already shipped; no additional defect reported. |
| #40 | Separate notification and while-open rules already confirmed by the reporter. |
| #41 | Partial incoming-call support uses Android's explicit incoming-call marker. Phone/WhatsApp/Teams/Messenger behavior still needs representative device notifications. |
| #42 | Recording-only tally remains unresolved: camera/microphone activity includes the viewfinder. No removal of the duration cap. |
| #45 | Optional charging/full colors, threshold, and finite pulses shipped in 1.0.12. Device sleep may delay signals. |
| #46 | Optional weekday schedules shipped; legacy daily settings and overnight start-day semantics retained. |
| #47 | Immediate known-blocker messages and fire-time rechecks shipped. A face-up phone before a delayed test does not prevent scheduling. |
| #48 | Finite DND activation indication shipped. It cannot identify Flip to Shhh as the specific cause of DND. |
| #51 | Reporter closed the issue September 8 and confirmed the 1.0.12 silent-filter/exclusion controls. |
| #53 | Optional finite reminder pulses shipped; the latest pending eligible notification is used. No guarantee of uninterrupted pulses in Doze or multi-notification rotation. |
| #54 | Complete preset copying, gradient endpoints and per-LED looks shipped. Existing saved-rule migration tests retained. |
| #55 | New Magisk 30.7 overnight report. Two concrete recovery blockers corrected. Incomplete diagnostics and no connected affected phone mean the overnight scenario itself is not proven fixed. Ask for a retest without clearing data and fresh LED diagnostics if it recurs. |
| #56 | Optional Shizuku-loss/reboot notice implemented; see the verification below. |

## Pull requests and Discussions

| Pull requests | Finding and disposition |
| --- | --- |
| #14 | R8 request already addressed in released code; entry-point retention checked again. |
| #27 | Notification-only installed apps already appear through remembered notification packages. The existing implementation preserves Forget remembered chats. |
| #30 | Exact typed time-slider entry already shipped and confirmed; PR closed. |
| #39 | Seven animations incorporated with preview/device parity and meaningful frame-shape tests. Contributor credited in the changelog. |
| #20 | Read the grouped-rule proposal and migration. Existing per-app notification/while-open rules and rule copying cover part of the intent. The new group schema, priority and editor require a dedicated migration/UI change; not included in this bug release. |
| #21 | Read the gauge proposal. Requested charging colors/thresholds already work; battery-level geometry is an additional animation, not a missing fix to those controls. Not included without its own behavior/device verification. |
| #15 | Hardware ColorSequence rewrite reviewed. Vendor allocator/service-crash behavior and firmware-specific effects require supported-device evidence before replacing the current renderer path. |
| #31, #43 | Read fork/desktop-manager changes and Discussion #44. Broad branding/platform changes and a Windows executable cannot be adopted as a verified Android fix; Fold/desktop support remains unverified. |
| #35, #38 | Read both persistent-alert proposals and all inline reviews. Index shifting, restart/screen-state behavior, persisted interval bounds and lifecycle need care. Existing finite pending reminders remain; permanent output and round-robin rotation were not added. |
| #1, #2, #4, #5, #6, #8, #33, #34, #50 | Dependency/action update proposals read. No reported fix requires these broad toolchain/library upgrades; retained the working versions for this release. They remain open for separate compatibility testing. |
| #3, #7 | Superseded closed dependency updates; no additional action. |
| #12, #18, #23, #24, #49 | Closed historical feature/setup/metadata/release PRs revisited. No new unresolved follow-up beyond the issue dispositions above. |

Discussions #10 and #13 contain confirmed ADB/root setup results. #16 documents an ADB setup method,
with a successful reply; its old broad process-kill command was not used. #44 introduces the fork
and notes possible battery drain; that is not evidence that current upstream has the same defect.
#52 is the Play closed-test announcement, with no new bug comments at audit time.

## Verification and review boundaries

- The full unit/build/lint gate passed with 337 tests, no failures or errors. Lint has zero errors;
  the eight additional warnings recommend SharedPreferences KTX syntax rather than flagging a
  correctness defect. Standalone helper compilation is separate from the APK build.
- New shell-fixture tests execute the real stop command against private fake process records.
  They verify exact TERM, unrelated PID reuse, different-instance rejection, another helper,
  and empty/unreadable identity. No host or phone process is signaled by those tests.
- Pattern tests compare preview/device frames at multiple speeds, brightness levels and times,
  verify saved-look round trips, and check dark rests, meter fill, bounce and convergence symmetry.
- Manager-loss tests include a failed/fenced Shizuku state with a dead manager; a live binder or
  active connection attempt does not count as manager loss.
- A completed Gemini 3.8 Flash source-fragment review prompted tighter root-start warning gating.
  Other findings were checked against full source: errors become ERROR/DENIED rather than an
  automatic retry loop, root priority is existing behavior, and stop commands already compare
  exact instance tokens. The proposed boot-receiver export change was rejected: Android explicitly
  permits system broadcasts to an unexported receiver ([official receiver documentation](https://developer.android.com/guide/topics/manifest/receiver-element)).
  Grok could not review because its account was out of usage. Neither route supplies device evidence.
- The connected Pixel was absent; a separate API 37.1 emulator was created for this release's
  installation and UI/boot-notification checks. It cannot verify LED hardware or Magisk/KernelSU.

The signed 1.0.12 to 1.0.13 emulator upgrade preserved all 16 preference entries from the prior QA
fixture (including rules, presets, schedule settings and the disabled master switch), with no
uninstall or data clearing. Publication, final checksums and boot-notification results follow below. The Play variant is handled in the existing PlayStore Release task,
keeping its application ID, feature restrictions and Play in-app updater.


## Signed candidate evidence

The final signed 1.0.13 APK was generated using the existing Keychain/keystore build script.
Certificate SHA-256 remains `15c1a4b5af54c3833e8d94582bddd985631cd007ca3f86d314d4be0bd5d9d9de`.
APK SHA-256: `469635ece13e3bf59fec50efc3c3bb3c45b45904386697b833cd4a80e627a5ac`.
The optimized APK preserves both privileged entry points. Full verification passed: **337 tests,
zero failures/errors; debug/release builds; lint zero errors and 55 warnings**.

A cold emulator boot with a fixture representing an opted-in, formerly connected Shizuku user
scheduled job 56, then delivered one recovery notification. Tapping it selected Setup. The ordinary
software reboot first stalled before Android completed boot; a cold emulator restart preserved the
fixture and completed the test. The successful boot check used the preceding signed candidate;
the final change only adds the failed-manager availability condition covered by the new test.
The final signed APK then installed successfully over that candidate without clearing data.
This is boot/job/UI evidence with fixture preferences, not a live Shizuku-loss or physical LED test.

No physical Pixel was attached for this cycle. Overnight Magisk/KernelSU recovery, the affected
stuck-LED case, recording-only tally, battery impact and broad incoming-call coverage remain open.


## Published result and follow-up

[1.0.13 experimental](https://github.com/DhananjayBhosale/hilight-studio/releases/tag/v1.0.13-experimental)
was published on September 12 from `716fc1fe0c4f4d0be852d51fe845309b12741455`.
[Hosted CI](https://github.com/DhananjayBhosale/hilight-studio/actions/runs/34674334378)
passed all test/build/lint, helper and optimized-entry-point steps. The downloaded public APK
matches the signed-candidate SHA-256 above and verifies with the permanent certificate.
The final emulator notification switch persisted its off value, and the scoped crash buffer
contained no HiLight crash. The dedicated emulator was stopped after QA; no physical-device
confirmation is implied.

After publication, contextual replies were posted to 15 issues and eight human-authored PRs.
They distinguish shipped behavior, reporter confirmation, partial coverage and deferred work;
no issue or PR was automatically closed. Already-closed confirmations and dependency bots were
not given redundant replies. F-Droid #22 was excluded after the user deferred GitLab work.

The existing PlayStore Release task received the exact source commit and a single bounded handoff
for Play integration, its own verification, signing and the existing Alpha-track submission.
It integrated and pushed Play source `11129e0983b950c2d3490cf0e573c50de326c79c`, passed 339 Play
and 337 GitHub-flavor unit tests, built and signed the Play artifacts, and uploaded code 14.
The official Play in-app updater and Play distribution restrictions remain in place.

The user completed the Console submission on September 12. Publishing overview subsequently
showed **Changes in review**, **Closed testing - Alpha**, **14 (1.0.13)** and **Start full rollout**,
with managed publishing off. Google quick checks were still running; review approval and tester
availability remain pending. The final saved Console notes use the user's edited English wording.
Exact artifact hashes and the Play verification/submission receipt are recorded in
`docs/releases/1.0.13-play.md` on `codex/play-store`. GitLab work remains deferred.
